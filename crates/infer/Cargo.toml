[package]
name = "infer"
version = "0.0.0"
edition = "2024"
authors = ["ryn <ryn@fppf.site>"]

description = "Type inference on the HIR"

[dependencies]
base = { path = "../base" }
span = { path = "../span" }
log = { path = "../log" }
syntax = { path = "../syntax" }
