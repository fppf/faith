type either 'l 'r = | Left 'l, Right 'r |

fn is_left x =
  case x {
    Left _ => true,
    Right _ => false,
  }

fn is_right x =
  case x {
    Left _ => false,
    Right _ => true,
  }

fn map l r x =
  case x {
    Left x => Left (l x),
    Right x => Right (r x),
  }
